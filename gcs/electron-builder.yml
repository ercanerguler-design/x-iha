appId: com.xiha.gcs
productName: X-IHA GCS
copyright: Copyright © 2026 X-IHA Aerospace

directories:
  output: dist
  buildResources: build

files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'

# serialport native binary'yi asar dışında tut
asarUnpack:
  - resources/**
  - '**/node_modules/@serialport/**'
  - '**/node_modules/serialport/**'

# ── Windows ───────────────────────────────────────
win:
  executableName: X-IHA-GCS
  target:
    - target: nsis
      arch: [x64]

nsis:
  artifactName: X-IHA-GCS-Setup-${version}.${ext}
  shortcutName: X-IHA GCS
  uninstallDisplayName: X-IHA GCS
  createDesktopShortcut: always
  oneClick: false
  allowToChangeInstallationDirectory: true

# ── macOS ─────────────────────────────────────────
mac:
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
  target:
    - target: dmg
      arch: [x64, arm64]

dmg:
  artifactName: X-IHA-GCS-${version}.${ext}

# ── Linux ─────────────────────────────────────────
linux:
  target:
    - AppImage
  maintainer: info@x-iha.com
  category: Utility
  description: X-IHA İnsansız Hava Aracı Yer Kontrol İstasyonu

appImage:
  artifactName: X-IHA-GCS-${version}.${ext}

npmRebuild: true

# ── Güncelleme Sunucusu (opsiyonel) ───────────────
publish:
  provider: github
  owner: ercanerguler-design
  repo: iha
